namespace PySharp.Roslyn.Shared.Utility;

internal static class GeneratedCode
{
    // Unified newline for every byte any generator emits: fixed LF keeps
    // output identical across platforms. Read-in normalization, the builder
    // and raw-string delimiting all reference this one constant.
    internal const string NewLine = "\n";
}
